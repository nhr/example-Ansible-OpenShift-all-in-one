---
image_prefix: registry.access.redhat.com/openshift3
#public_ssh_key_path: /path/to/.ssh/ssh_key.pub

# If you have any post-cluster deployments you want to launch, you can list them here and add plays to launch them 
# a cakephp-mysql example app will launch from 'devenv-launch.yml')
deploy_components: 
- example_app
- my_next_app
- another_app

# fill in these vars for RedHat Subscription Manager
subscription_username: fillin 
subscription_password: fillin
subscription_pool_id: fillin # Pool ID that includes OpenShift Container Platform
